#include <stdio.h>

int main(){
    float a,b;
    // float mul;

    printf("Enter the numbers :");
    scanf("%f%f",&a,&b);

    // mul= a*b;

    printf("the product of the two number is : %.2f ",a*b);

    return 0;
}