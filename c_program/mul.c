#include <stdio.h>
int main(){
    int one,two,mul;

    printf("Enter the number :");
    scanf("%d%d",&one,&two);
    mul =one*two;

    printf("the multiplictaion of the numbers is :%d",mul);

    return 0;

}