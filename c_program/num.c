#include <stdio.h>

int main(){

    int num;
    printf("Enter the number :");
    scanf("%d",&num);

    if (num >100)
    {
        printf("the number is greater than 100");
    }
    else{
        printf("the numberb is less than 100");
    }

    return 0;
    
}