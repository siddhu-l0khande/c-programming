#include <stdio.h>

int main(){

     int a,b;
     printf("Enter the numbers :");
     scanf("%d%d",&a,&b);

     if (a>b)
     {
      printf("The number a=%d is greater ",a);
     }
     else{
        printf("the number b=%d is graeter ",b);
    
     }
     return 0 ;
     

}